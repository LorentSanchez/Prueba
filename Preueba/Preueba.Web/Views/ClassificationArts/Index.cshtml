@model IEnumerable<Prueba.Web.Models.ClassificationArt>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NameArtc)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameArtc)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ArtcId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ArtcId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ArtcId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
